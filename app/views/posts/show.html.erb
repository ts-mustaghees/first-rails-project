<% provide(:title, @post.title) %>

<p id="notice"><%= notice %></p>

<div class="container">
    <h2><%= @post.title %></h2>
    <p><%= @post.body %></p>
    <div>Published on <%= @post.created_at %></div>
</div>