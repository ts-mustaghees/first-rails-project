<% provide(:title, @post.title) %>

<div class="container mt-5">
    <h2><%= @post.title %></h2>
    <div>Published on <%= @post.created_at.strftime("%-d/%-m/%y: %k:%M %p") %></div>

    <p class="mt-3"><%= @post.body %></p>
</div>