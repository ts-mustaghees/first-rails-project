<% provide(:title, "Update Profile") %>
<% provide(:submit_btn_text, "Update Profile") %>

<div class="container mt-5">
  <h1>Editing <%= @user.name %></h1>

  <%= render 'form', form_action: user_path(@user) %>
</div>
